# frozen_string_literal: true

RSpec.describe TweetStorm do
  it "has a version number" do
    expect(TweetStorm::VERSION).not_to be nil
  end

  it "does something useful", skip: true do
    expect(false).to eq(true)
  end
end
